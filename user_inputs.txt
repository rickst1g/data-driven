1.1 I verified that Python and Jupyter were installed correctly on my machine. Python version: 3.14.3. Jupyter version: 7.5.5.

1.2 I installed the required libraries for the project, including datasets, pandas, matplotlib, seaborn, and pyarrow.

2.1 I loaded the Amazon Reviews dataset from Hugging Face using the All_Beauty review split and confirmed it could be accessed successfully.

2.2 I used a sample limit of 100 and a for loop to collect the first 100 review records so the dataset would be easier to work with.

2.3 I converted the collected review data into a pandas DataFrame called reviews_df and used head() to inspect the first few rows.

2.4 I set Items_To_Print to 10 so I could limit the displayed output and make the results easier to read.

3.1 I loaded the metadata dataset for All_Beauty and limited the collection to 100 items, similar to the reviews dataset.

3.2 I examined the metadata DataFrame to better understand its columns and structure.

3.3 I used exception handling in the get_product function so missing product matches would not cause the notebook to crash.

4.1 I reviewed the columns in both datasets using the .columns attribute.

4.2 I used a loop to print the column names from both DataFrames as lists for easier visibility.

4.3 I used .dtypes and .info() to inspect the data types, non-null counts, and overall structure of each DataFrame.

5.1 I cleaned the metadata by keeping only rows with a title, an average rating of at least 4.5, and a valid price. I also kept only the title, average_rating, and price columns.

5.3 I created a calculate_percentage function that returns part divided by whole.

5.4 I saved the cleaned dataset as a CSV file named top_products.csv.

5.5 I saved the cleaned dataset as a Parquet file named top_products.parquet.

5.6 I created a list called top_rated_titles and appended the titles from the cleaned DataFrame.

6.1 I created a histogram of product prices using the cleaned DataFrame.

6.2 I enabled the KDE option in the histogram to better visualize the price distribution.

6.3 I created a scatterplot using price on the x-axis and average_rating on the y-axis.

6.4 I applied a stricter rating cutoff of 4.7 to focus on highly rated products.

6.5 I created a sorted DataFrame of highly rated products and visualized the relationship between price and average rating with a scatterplot.